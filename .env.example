# Copy to .env (do NOT commit .env)
# Replace with your OpenRouteService API key
ORS_API_KEY=your_openrouteservice_api_key_here
# Optional: local proxy URL for OpenRouteService (e.g. http://localhost:8080)
# If set, the app will use this proxy for routing requests instead of calling ORS directly.
ORS_PROXY_URL=http://localhost:8080
